apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: restaurant-pos
spec:
  selector:
    app: backend # Routes to backend pods
  ports:
    - name: http
      protocol: TCP
      port: 8000 # Service port
      targetPort: 8000 # Container port
  type: ClusterIP # Internal only (frontend will call it)
